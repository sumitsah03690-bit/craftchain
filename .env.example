# ──────────────────────────────────────────────
# CraftChain — Environment Variables
# ──────────────────────────────────────────────
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# ──────────────────────────────────────────────

# MongoDB connection string (Atlas or local)
# Example: mongodb+srv://user:pass@cluster.mongodb.net/craftchain
MONGODB_URI=

# Secret used to sign JSON Web Tokens (pick any long random string)
JWT_SECRET=change-me-to-a-random-secret

# Port for the Express API server (default: 4000)
PORT=4000

# URL of the React client (used for CORS, redirects, etc.)
CLIENT_URL=http://localhost:5173
