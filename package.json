{
  "name": "craftchain",
  "version": "0.1.0",
  "private": true,
  "description": "CraftChain — hackathon mono-repo (server + client)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "minecraft-assets": "^1.17.0"
  }
}
